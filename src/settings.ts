/**
 * Name that homebridge-config-ui-x / the Homebridge server uses to load this plugin's platform.
 * Must match the "platform" field used in config.json and the name registered in index.ts.
 */
export const PLATFORM_NAME = 'ReolinkHksvDoorbell';

/**
 * Must match the "name" field in package.json.
 */
export const PLUGIN_NAME = 'homebridge-reolink-hksv-doorbell';
